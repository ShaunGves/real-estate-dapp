{
  "name": "real-estate-contracts",
  "version": "1.0.0",
  "description": "Tokenized real-estate crowdfunding smart contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:ganache": "hardhat run scripts/deploy.js --network ganache",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "seed:local": "hardhat run scripts/listSampleProperty.js --network localhost",
    "seed:ganache": "hardhat run scripts/listSampleProperty.js --network ganache",
    "seed:sepolia": "hardhat run scripts/listSampleProperty.js --network sepolia",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
